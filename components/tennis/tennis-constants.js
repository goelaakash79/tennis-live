/** Must match the `setInterval` that calls `refreshLive` in `TennisApp`. */
export const SCORE_POLL_MS = 15_000;
/** Live cards show a gradient ring in the last N ms before a scheduled poll. */
export const SCORE_SOON_MS = 2_000;
